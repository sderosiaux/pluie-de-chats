#!/usr/bin/env sh
# Pre-commit : typecheck + lint avant chaque commit
# Bypass : git commit --no-verify
set -e

echo "▶ typecheck..."
npm run typecheck

echo "▶ lint..."
npm run lint

echo "✓ pre-commit OK"
